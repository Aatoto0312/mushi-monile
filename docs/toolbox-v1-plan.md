# Toolbox v1 実装計画

1. Registry→catalog派生adapterとfuture/500件RED testを追加。
2. Toolbox coreに動的filter、sort、Deck validation、複製、破損Deck隔離を追加。
3. 既存HTML/CSS/coreを利用したRegistry駆動v1 appを接続。
4. 共通Deck runtimeで保存DeckをCardDefinition配列へ展開。
5. Battle開始選択と双方向導線を追加。
6. SET1 user Deckの実Engine開始、全回帰、syntax/diff/browser smokeを実施。

commit / pushは行わない。
